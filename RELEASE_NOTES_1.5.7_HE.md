# מסע הניקוד 1.5.7 — שלב א׳: ניקוי ארכיטקטורת הליבה

## מה השתנה

- נוסף `GameSessionManager` כנקודת הרכבה אחת למנהלי מצב המשחק.
- אתחול וניקוי של תורים, זרימת משחק, שאלות, ניקוד, סטטיסטיקות, נחיתה וניצחון מרוכזים כעת במקום אחד.
- `GameScene` צומצמה ואינה יוצרת עוד כל מנהל מצב בנפרד.
- מחזור החיים של מנהלי הליבה הפך עקבי: `start()` ו־`destroy()` מרכזיים.
- נשמרה זרימת המשחק הקיימת ללא שינוי בחוקי הקובייה, התנועה, השאלות או הניקוד.

## מטרת השלב

זהו הצעד הראשון בשלב א׳ — השלמת הליבה. המטרה היא להפוך את `GameScene` למתאמת בלבד ולצמצם תלות ישירה בין רכיבי המשחק.
