# מסע הניקוד 1.5.12 — A5.2 מנגנוני התאוששות

## מה נוסף

- נוסף `RecoveryManager` מרכזי שמנהל שעוני בטיחות לפעולות אוטומטיות במחזור המשחק.
- גלגול קובייה שלא מסתיים בתוך 5 שניות מתאפס בצורה בטוחה.
- תנועה שלא מסתיימת בתוך 45 שניות מבוטלת ומחזירה את המשחק למצב `READY`.
- פתרון תור שנתקע מקבל מנגנון התאוששות ייעודי.
- זמן החשיבה בשאלה אינו מוגבל ואינו מפעיל Watchdog.
- בעת התאוששות מתנקים תנועה פעילה, שאלה פתוחה, נתוני נחיתה ונעילות קובייה.
- נוסף `DiceManager.recover()` שמבטל טיימרים פנימיים ומחזיר את הקובייה למצב פעיל.
- המשתמש מקבל הודעת התאוששות ידידותית במקום שהמשחק יישאר תקוע.
- נוספה ספירת ניסיונות התאוששות וזיהוי של תקלות חוזרות.
- לוגר היציבות הורחב עם אזור `RECOVERY` ועם רמת `error`.
- מספר הגרסה עודכן ל־1.5.12.

## בדיקות שבוצעו

- בדיקת מבנה ZIP.
- בדיקת תחביר TypeScript לכל קובצי המקור באמצעות transpile בלבד.
- בדיקה ממוקדת של RecoveryManager עם שעון מדומה: הפעלה, ביטול, איפוס והפעלת callback.

## מגבלה בסביבת הבנייה

בדיקת `tsc` ובניית Vite מלאה עדיין תלויות בחבילות הפיתוח הזמינות בסביבת ההרצה. לכן הגרסה אינה מוצגת כ־build מלא שנבדק על Windows.
