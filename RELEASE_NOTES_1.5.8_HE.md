# מסע הניקוד 1.5.8 — Core Completion A2

## מה השתנה

- נוסף `GameplayCoordinator` שמרכז את מחזור המשחק הפעיל: זריקת קובייה, תנועה, נחיתה, שאלה, תשובה וסיום תור.
- `GameScene` צומצמה וכעת מתמקדת בהרכבת הרכיבים, ניווט בין מסכים ותצוגת הרקע.
- מנגנון ההתאוששות מתנועה שנתקעה עבר למתאם המשחק המרכזי.
- פתיחת שאלות וטיפול בתשובות הועברו מהסצנה למתאם המשחק.
- סיום המשחק עובר במסלול יחיד: `WinManager` ← `GameplayCoordinator` ← חלון הניצחון.
- הוסרו `EventBus` ו־`GameEvents` הישנים, שלא היו בשימוש, כדי למנוע שתי שיטות אירועים מקבילות.
- נוסף ניקוי מרכזי של קובייה, טיימר התנועה ותנועה פעילה בעת סגירת המשחק.

## מטרת השלב

להשלים את A2 — ניקוי הארכיטקטורה — ולהשאיר את `GameScene` כמנצחת על הרכיבים במקום כמקום שבו מתבצעת לוגיקת המשחק.

## בדיקות

- נבדקה היעדרות של ייבואים ל־EventBus הישן.
- בוצעה בדיקת TypeScript לפרויקט ככל שהתלויות המקומיות מאפשרות.
- נבדקה תקינות מבנה קובץ ה־ZIP.
