# מסע הניקוד 1.0.0 GOLD

גרסת ההפצה הרשמית הראשונה של **מסע הניקוד**.

## תוכן הגרסה

- שישה לוחות לימודיים פעילים.
- חמישה מסלולי למידה ללא נחשים וסולמות.
- לוח "מסע הניקוד" עם ארבעה סולמות וארבעה נחשים.
- 210 תחנות משחק, 169 קבוצות שאלות ו-612 שאלות.
- שאלות לפי ההברה המנוקדת שעליה נחת השחקן.
- ניסיון נוסף לאחר טעות ראשונה לפני חשיפת הפתרון.
- תמונות מקדימות לכל הלוחות.
- לוגו עגול ללא מסגרת בפינה הימנית העליונה.
- קובייה בעלת התפלגות מותאמת למשחק לימודי.
- תמיכה ב-Electron וביצירת מתקין Windows מסוג NSIS.

## בדיקות שבוצעו בסביבת ההכנה

- `npm run qa` עבר בהצלחה.
- אומתו כל קובצי הלוחות, התמונות המקדימות והמשאבים.
- אומתה גרסת `1.0.0` בקובץ ההפצה.
- נבדקה תקינות ארכיון ה-ZIP.

## בנייה והרצה ב-Windows

```powershell
npm install
npm run qa
npm run desktop
```

ליצירת מתקין:

```powershell
npm run dist:win
```

> הערה: Build מלא לא בוצע בסביבת ההכנה עקב חוסר זמינות של `@types/node` במאגר החבילות הפנימי. יש לבצע את בניית Electron במחשב Windows המחובר ל-npm.
