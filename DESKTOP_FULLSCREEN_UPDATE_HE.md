# גרסת תוכנה ומסך מלא

## תנועה חדשה
לאחר זריקת הקובייה מודגשת משבצת היעד היחידה. גוררים את החייל פעם אחת ישירות אליה. אם החייל משוחרר במקום אחר, הוא חוזר לנקודת המוצא וניתן לנסות שוב ללא זריקה נוספת.

## הפעלה כתוכנת Windows

```bash
npm install
npm run desktop
```

היישום נפתח אוטומטית במסך מלא, ללא ממשק דפדפן.

## יצירת מתקין Windows

```bash
npm install
npm run dist:win
```

המתקין ייווצר בתיקייה `release`.

## מקש תחזוקה
`F11` מחליף בין מסך מלא לחלון.
