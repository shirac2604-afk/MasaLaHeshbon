# מסע הניקוד 1.5.0 — Core Engine, שלב א׳

## מה השתנה

- `TurnManager` נבנה מחדש כמכונת מצבים מרכזית.
- נוספו מצבי משחק מוגדרים: מוכן, גלגול, תנועה, שאלה, עיבוד תוצאה וסיום.
- נוסף `GameFlowManager` שמאשר כל מעבר ומונע פעולות כפולות או מעבר לא חוקי.
- הקובייה מודיעה למנוע על תחילת הגלגול לפני הפעלת האנימציה.
- תנועה מתחילה רק לאחר שהגלגול אושר והסתיים.
- שאלה נפתחת רק לאחר שהמנוע אישר נחיתה חוקית.
- תשובה מתקבלת פעם אחת בלבד ולאחריה התור מתקדם לשחקן הבא.
- מנגנון ההתאוששות מתנועה שלא הושלמה מחזיר את המשחק למצב מוכן באופן מסודר.
- ה-HUD מקבל את מצב המשחק ישירות ממנוע הזרימה.

## בדיקות שבוצעו

- מכונת המצבים קומפלה בנפרד במצב TypeScript strict.
- בוצעה בדיקת זרימה אוטומטית לשני שחקנים:
  מוכן → גלגול → תנועה → שאלה → עיבוד → שחקן הבא → מוכן.
- מבנה ה-ZIP נבדק לאחר האריזה.

## מגבלת סביבת הבדיקה

לא ניתן היה לבצע `npm install` מלא משום שמאגר החבילות בסביבה מחזיר 404 עבור `@types/node`. לכן Build מלא של Phaser/Vite לא הושלם בסביבה זו.
