# מסע הניקוד 1.9.0 RC3.8.1

## תיקון Build

- תוקן אירוע שגיאת טעינת תמונות ב־`BoardLoader`.
- הוחלף הקבוע שאינו קיים `Phaser.Loader.Events.LOAD_ERROR` בשם האירוע הנתמך `loaderror`.
- לא בוצע שינוי בהתנהגות המשחק או בתוכן הלוחות.

## בדיקה מומלצת

```powershell
npm install
npm run desktop
```
