# MasaHaNikud – UI Integration

בוצע חיבור מלא של רכיבי ה-UI לסצנת המשחק:

- `ScorePanel` מחובר לתור, לניקוד ולתוצאת הקובייה.
- `QuestionPopup` מחובר למודל השאלות ולבדיקת תשובות לפי מזהה התשובה.
- תשובה נכונה מעניקה 10 נקודות.
- הודעות `alert()` הוסרו והוחלפו במשוב בתוך המשחק.
- `WinPopup` מחובר לאירוע הניצחון, לניקוד הסופי, למשחק חדש ולחזרה לתפריט.
- נוסף ניקוי מסודר של מאזיני EventBus בעת סגירת הסצנה.
- תוקנו חוסרים והתנגשויות TypeScript שהיו בפרויקט המקורי, כולל GameConfig, AssetKeys ומודלים.

## התקנה והרצה

```bash
npm install
npm run dev
```

## בניית גרסת production

```bash
npm run build
```

הערה: תיקיית `node_modules` אינה כלולה בחבילה המעודכנת. יש להריץ `npm install` לאחר החילוץ.
