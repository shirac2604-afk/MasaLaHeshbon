# מסע הניקוד 1.9.0 RC4.1 — שאלות לפי הברה

- כל משבצת לימודית מקושרת כעת לקבוצת שאלות לפי ההברה המנוקדת שמופיעה עליה, ולא לפי מספר המשבצת.
- לכל שאלה נוסף שדה `symbol` שמזהה את ההברה שאליה היא שייכת.
- לכל משבצת נוסף יעד למידה מפורש.
- קבוצות השאלות משתמשות במזהים יציבים המבוססים על תווי ההברה.
- בעת נחיתה, המשבצת החדשה מודגשת מיד לפני פתיחת השאלה.
- בדיקת התוכן מאמתת שההברה שעל המשבצת תואמת לכל השאלות בקבוצה.
