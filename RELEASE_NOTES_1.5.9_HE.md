# מסע הניקוד 1.5.9 — Core Completion A3

שלב A3 משלים ומייצב את מנהלי הממשק, החלונות, האנימציות והצלילים.

## שינויים

- `PopupManager` מנהל כעת מצב חלון פעיל יחיד ומונע חפיפה בין שאלה, יציאה וניצחון.
- נוספו `closeAllImmediately`, `isBlocking` וסטטוס `PopupKind` לניהול בטוח של חלונות.
- כשל בפתיחת חלון שאלה מחזיר את זרימת המשחק למצב מוכן במקום להשאיר תור נעול.
- `AnimationManager` מנהל אובייקטים זמניים, ניקוי Tweens והעדפת "הפחתת תנועה".
- `UIManager` מציג מצב השהיה ומצב צליל באופן עקבי ומנקה את מנהל האנימציות בסגירה.
- תוקן מצב שבו ביטול חלון היציאה השאיר את הקובייה נעולה.
- נוספה השהיה אמיתית בין תורים: הקובייה ננעלת בהשהיה ונפתחת מחדש בהמשך.
- `SoundManager` עוקב אחר כל הצלילים המתוזמנים ומבטל אותם בעת השתקה.
- מנגנון שחרור האודיו לאחר מחוות משתמש הוקשח לדפדפן ול-Electron.
- מספר הגרסה עודכן ל-1.5.9.

## גבולות הבדיקה

בוצעה בדיקת TypeScript מקומית באמצעות הקומפיילר המותקן בסביבה. Build מלא של Vite תלוי בחבילות הפרויקט ואינו נחשב מאומת עד להרצת `npm install` ו-`npm run build` בסביבה עם גישה תקינה למאגר החבילות.
