# מסע הניקוד 1.5.13 — A5.3 ניקוי משאבים

## מה השתנה

- נוסף `ResourceCleanupManager` שמרכז את פירוק רכיבי סצנת המשחק ומבטיח שכל פעולת ניקוי מתבצעת פעם אחת בלבד.
- הניקוי מתבצע בסדר הפוך ליצירת הרכיבים, כדי למנוע גישה של רכיב פעיל לתלות שכבר פורקה.
- בעת יציאה או הפעלה מחדש מנוקים במפורש:
  - מאזיני קלט של הסצנה.
  - טיימרים ואירועים מתוזמנים.
  - Tweens פעילים.
  - חלונות קופצים ואובייקטי HUD.
  - תנועה פעילה וסימוני יעד.
  - קובייה ושחקנים.
  - צלילים פעילים וצלילים מתוזמנים.
- `AnimationManager` עוקב כעת גם אחר טיימרים של משוב במצב הפחתת תנועה ומבטל אותם בזמן פירוק.
- `MovementManager` מבטל callbacks ישנים לאחר התאוששות או סגירת סצנה ומנקה הודעת סולם/נחש פעילה.
- `PlayerToken` עוצר את כל האנימציות ומסיר מאזינים לפני השמדה.
- `SoundManager` עוקב אחר Oscillator/Gain פעילים ומספק `stopAll()` לעצירה מיידית ללא סגירת AudioContext המשותף.
- נוסף אזור לוג `CLEANUP` לבדיקת סדר הפירוק ולאיתור כשלי ניקוי.
- מספר הגרסה עודכן ל־1.5.13.

## מטרת השלב

למנוע callbacks מאוחרים, מאזינים כפולים, אנימציות שנותרו פעילות וצלילים שממשיכים לאחר מעבר בין מסכים או התחלת משחק חדש.

## בדיקות

- בדיקת תחביר לכל קובצי TypeScript.
- בדיקת TypeScript מלאה ככל שהתלויות הזמינות בסביבה מאפשרות.
- בדיקת תקינות ארכיון ZIP.
