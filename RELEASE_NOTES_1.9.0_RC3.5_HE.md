# מסע הניקוד 1.9.0 RC3.5 — תיקון Build TypeScript

## תיקונים

- עודכנו קובצי הלוחות הישנים `kamatz.ts`, `segol.ts` ו־`shuruk.ts` למבנה `Tile` העדכני.
- נוספו לכל משבצת `centerX`, `centerY` ו־`questionGroup`.
- תוקנה בדיקת קבוצות השאלות ב־`BoardPackRegistry` כך ש־TypeScript מצמצם את הטיפוס ל־`string` באופן תקין.
- צבע הכותרת בחלון „אודות” הומר ממספר לצבע CSS תקין.
- מספר הגרסה עודכן ל־`1.9.0-rc.3.5`.

## בדיקות

- בדיקת התוכן עברה: 6 לוחות, 240 משבצות, 240 קבוצות ו־702 שאלות.
- ניסיון התקנת התלויות בסביבת העבודה נעצר משום שהמאגר הפנימי אינו מספק את `@types/node`.
- לכן יש להריץ במחשב Windows:
  - `npm install`
  - `npm run desktop`
